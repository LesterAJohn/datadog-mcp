# Vault Production Assets

This folder provides production Vault conversion and bootstrap helpers used by `datadog-mcp`.

Use these assets when promoting from development Vault setup to production-grade unseal/raft flows.

Keep all Datadog API credentials and MCP HTTP tokens in Vault when running in production.
